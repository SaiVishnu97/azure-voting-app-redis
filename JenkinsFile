@Library('https://github.com/devbyaccident/demo-shared-pipeline')

echoPipeline
{
message="I tried to msg 1000times."
}
pipeline {
    agent any

    stages {
        stage('Verify Build') {
            steps {
                echo "$GIT_BRANCH"
            }
        }
        stage('call hello world from a shared library') {
         steps {
           script
           {
           helloWorld()
           }
         }
      }
    }
}
